[project]
name = "home-lab"
version = "0.1.0"
description = "Home lab automation using Ansible to provision and manage Raspberry Pi CM5 cluster"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ansible>=11.9.0",
    "ansible-lint>=25.8.1",
    # Required by the json_query filter the grafana.grafana role uses to
    # provision dashboards (grafana_dashboards in group_vars/monitoring).
    "jmespath>=1.0.0",
    "pre-commit>=4.3.0",
    "yamllint>=1.37.1",
]
