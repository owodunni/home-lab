---
galaxy_info:
  role_name: backup_verify
  author: home-lab
  description: >-
    Generic, non-destructive verification that a service's backups exist and are
    fresh. Loops the service's `backups` manifest and dispatches each entry to a
    per-type handler (tasks/<type>.yml).
  license: MIT
  min_ansible_version: "2.15"
dependencies: []
